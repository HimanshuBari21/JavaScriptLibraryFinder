import SearchBox from "@/components/SearchBox";

const Home = () => {
  return (
    <section>
      <div className="container">
        <SearchBox />
      </div>
    </section>
  );
};

export default Home;
